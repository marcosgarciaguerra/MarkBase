services:

  # ── Reverse Proxy ──────────────────────────────────────
  caddy:
    image: caddy:2-alpine
    container_name: nas-caddy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    environment:
      NAS_DOMAIN: ${NAS_DOMAIN}
      NEXTCLOUD_SUBDOMAIN: ${NEXTCLOUD_SUBDOMAIN}
      IMMICH_SUBDOMAIN: ${IMMICH_SUBDOMAIN}
      JELLYFIN_SUBDOMAIN: ${JELLYFIN_SUBDOMAIN}
    volumes:
      - ./caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - ${NAS_DATA_PATH}/docker/caddy/data:/data
      - ${NAS_DATA_PATH}/docker/caddy/config:/config
    networks:
      - nas-net

  # ── Nextcloud ──────────────────────────────────────────
  mariadb:
    image: mariadb:11.4
    container_name: nas-mariadb
    restart: unless-stopped
    command: --transaction-isolation=READ-COMMITTED --log-bin=binlog --binlog-format=ROW
    env_file: .env
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
    volumes:
      - ${NAS_DATA_PATH}/docker/mariadb:/var/lib/mysql
    networks:
      - nas-net
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  redis-nextcloud:
    image: redis:7-alpine
    container_name: nas-redis-nextcloud
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - ${NAS_DATA_PATH}/docker/redis:/data
    networks:
      - nas-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  nextcloud:
    image: nextcloud:29-apache
    container_name: nas-nextcloud
    restart: unless-stopped
    depends_on:
      mariadb:
        condition: service_healthy
      redis-nextcloud:
        condition: service_healthy
    env_file: .env
    environment:
      MYSQL_HOST: mariadb
      MYSQL_DATABASE: ${MYSQL_DATABASE}
      MYSQL_USER: ${MYSQL_USER}
      MYSQL_PASSWORD: ${MYSQL_PASSWORD}
      REDIS_HOST: redis-nextcloud
      NEXTCLOUD_ADMIN_USER: ${NEXTCLOUD_ADMIN_USER}
      NEXTCLOUD_ADMIN_PASSWORD: ${NEXTCLOUD_ADMIN_PASSWORD}
      NEXTCLOUD_TRUSTED_DOMAINS: ${NEXTCLOUD_SUBDOMAIN}.${NAS_DOMAIN}
      OVERWRITEPROTOCOL: https
      OVERWRITEHOST: ${NEXTCLOUD_SUBDOMAIN}.${NAS_DOMAIN}
    volumes:
      - ${NAS_DATA_PATH}/nextcloud:/var/www/html
    networks:
      - nas-net

  # ── Immich ─────────────────────────────────────────────
  postgres:
    image: ghcr.io/immich-app/postgres:16-vectorchord0.4.3-pgvector0.8.1-pgvectors0.2.0
    container_name: nas-postgres
    restart: unless-stopped
    env_file: .env
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_INITDB_ARGS: --data-checksums
    volumes:
      - ${NAS_DATA_PATH}/docker/postgres:/var/lib/postgresql/data
    command:
      - postgres
      - -c
      - shared_preload_libraries=vchord.so,vectors.so
      - -c
      - shared_buffers=256MB
      - -c
      - max_wal_size=2GB
    shm_size: 128mb
    networks:
      - nas-net
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\" || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 90s

  redis-immich:
    image: redis:7-alpine
    container_name: nas-redis-immich
    restart: unless-stopped
    command: redis-server --save 60 1 --loglevel warning
    volumes:
      - ${NAS_DATA_PATH}/docker/redis-immich:/data
    networks:
      - nas-net
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s

  immich-server:
    image: ghcr.io/immich-app/immich-server:release
    container_name: nas-immich-server
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      redis-immich:
        condition: service_healthy
    env_file: .env
    environment:
      DB_HOSTNAME: postgres
      DB_PORT: 5432
      DB_USERNAME: ${POSTGRES_USER}
      DB_PASSWORD: ${POSTGRES_PASSWORD}
      DB_DATABASE_NAME: ${POSTGRES_DB}
      REDIS_HOSTNAME: redis-immich
      REDIS_PORT: 6379
      UPLOAD_LOCATION: /usr/src/app/upload
      IMMICH_MACHINE_LEARNING_URL: http://immich-ml:3003
    volumes:
      - ${NAS_DATA_PATH}/immich:/usr/src/app/upload
    networks:
      - nas-net

  immich-ml:
    image: ghcr.io/immich-app/immich-machine-learning:release
    container_name: nas-immich-ml
    restart: unless-stopped
    volumes:
      - ${NAS_DATA_PATH}/docker/immich/model-cache:/cache
    networks:
      - nas-net

  # ── Jellyfin ───────────────────────────────────────────
  jellyfin:
    image: jellyfin/jellyfin:latest
    container_name: nas-jellyfin
    restart: unless-stopped
    env_file: .env
    environment:
      TZ: ${TZ}
    volumes:
      - ${NAS_DATA_PATH}/docker/jellyfin/config:/config
      - ${NAS_DATA_PATH}/docker/jellyfin/cache:/cache
      - ${NAS_DATA_PATH}/media:/media:ro
    networks:
      - nas-net

networks:
  nas-net:
    driver: bridge
